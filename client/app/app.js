// app.js file defines our app, and that's it.

// next line tells angular to create a module called 'cocktail' with no dependencies
// (omitting the array would tell it to go find the module called 'cocktail')
angular.module('cocktail', []);
